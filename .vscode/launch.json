{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/tab-completion"],
      "outFiles": ["${workspaceFolder}/tab-completion/out/**/*.js"],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
